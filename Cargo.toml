[package]
name = "racing_game"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.16"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 开发时优化编译速度
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
